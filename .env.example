# Variables de entorno para Railway / Local
PORT=3000
NODE_ENV=production

# Base de datos (Opcional - PostgreSQL / MySQL de Railway)
# Si no se define DATABASE_URL, el servidor usará automáticamente el almacén de datos integrado
DATABASE_URL=postgresql://postgres:password@roundhouse.proxy.rlwy.net:12345/railway
